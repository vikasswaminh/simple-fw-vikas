/target/
node_modules/
front/dist/
e2e-artifacts/
*.iso
output/
*.vmdk
*.vmem
*.lck
*.dmp

# Release signing material — the private key file. NEVER commit.
*.key
quickfw-release.key

# Cargo.lock is intentionally tracked for reproducible builds of binaries
# Cargo.lock
